# Generated rule index

Generated by `npm run docs:generate` from `rules/`. Do not edit by hand.

18 rules across 15 files. Every rule names at least one fixture that
demonstrates it, which `scripts/validate-rules.ts` enforces: a rule nothing demonstrates is
an assertion nothing checks.

| Rule | Severity | Statement | Fixtures |
| --- | --- | --- | --- |
| dependency/artifact-dependency | MUST | A dependency whose target is an artifact or executable MUST be established from a digest match or from a build input declared by the subject, and MUST NOT be established from artifact size, media type, module name, version string or any other metadata similarity. A dependency of class EXTERNAL MUST record that its target lies outside the observable boundary and MUST NOT be reported as VERIFIED. | provenance/mismatched-wasm, invalid/malformed-dependency |
| dependency/contract-dependency | MUST | A dependency of class CONTRACT MUST name the network its target was observed on. Its basis MUST be one of an observed invocation, an observed event, an attested statement, or a reference the subject's own source or build demonstrably contains. When the basis is an inferred interface, the confidence MUST NOT exceed LOW_CONFIDENCE and the dependency MUST NOT appear among the observed facts of a report. When the basis is an observed invocation, the underlying transaction MUST be recorded as successful. | contract-dependency/direct, invalid/malformed-dependency |
| dependency/direct-dependency | MUST | Every dependency MUST state how it was established and MUST reference at least one evidence record. A dependency MUST NOT be asserted because two entities share a name, mention one another, exist in the same ecosystem, share an operator, or carry similar metadata. When a dependency is represented as an edge whose relationship is DEPENDS_ON, the edge MUST carry a basis. A dependency of class CONTRACT or RUNTIME MUST name the network it was observed on, a dependency of class RUNTIME MUST be established from a transaction recorded as successful, and a dependency edge that reached a network observation MUST record firstObserved with lastObserved not preceding it. | contract-dependency/direct, invalid/malformed-dependency, invalid/broken-edge |
| dependency/edge-reference-integrity | MUST | Every edge in a graph MUST have both of its endpoints resolve to a node identifier declared in that graph. A producer MUST NOT emit an edge whose endpoint is absent, and a consumer MUST NOT silently drop such an edge. | contract-dependency/mixed, invalid/broken-edge |
| dependency/transitive-dependency | MUST | A dependency of class TRANSITIVE MUST carry the intermediate entities between its source and target, in order, with at least one entry. Its aggregated confidence MUST equal the minimum confidence ordinal among the hops it traverses. A dependency set MUST list every edge that has a dependency class in exactly one of its direct or transitive partitions, MUST set maxDepth, MUST set truncated with a truncationReason when traversal stopped early, and MUST report any cycle it detected with the edge identifiers that form it rather than removing or collapsing the cycle. | contract-dependency/transitive, contract-dependency/mixed |
| identity/artifact-identity | MUST | Every artifact MUST carry a digest with an algorithm permitted for its declared type. An artifact MUST NOT be identified by a locator, a file name, a version string or a revision alone, and a WASM artifact MUST be digested with sha256. | minimal/artifact, provenance/mismatched-wasm |
| identity/contract-identity | MUST | A contract identity MUST declare a contract address, the network it was observed on and an identity version. A document MUST NOT treat an address as sufficient to identify a contract, and a consumer MUST NOT merge two identities that differ in network or in identity version into one entity. | minimal/contract, invalid/malformed-contract |
| identity/wasm-identity | MUST | A deployed executable identity MUST be established by the digest the network reports for the contract, and MUST be recorded separately from any claimed source repository, source revision, build artifact or build configuration. A document MUST NOT record a source revision as though it were the identity of the deployed executable, and MUST NOT infer a source revision from a matching repository name. | provenance/verified-build, provenance/mismatched-wasm |
| impact/change-impact | MUST | An impact finding whose impact type set includes CHANGE MUST carry a changeType. A snapshot diff MUST be produced by canonical comparison, MUST NOT report a difference caused only by ordering or by a volatile field, MUST classify every difference with a category and a change type, and MUST state a reason for each. A diff whose snapshots are not comparable MUST set comparable to false, MUST supply an incomparableReason, and MUST have an empty changes array. A diff summary MUST agree with its own changes array. UPGRADED and DOWNGRADED MUST be used only where the referenced ecosystem establishes an ordering, and MODIFIED MUST be used otherwise. | impact/direct-impact, invalid/invalid-provenance |
| impact/deployment-impact | MUST | An impact finding whose affected entity is a deployment MUST include DEPLOYMENT in its impact type set, MUST identify the ledger at which that deployment was recorded, and MUST NOT name a deployment whose status is UNCONFIRMED, FAILED or UNKNOWN. An impact finding whose affected entity is a contract identity MUST include CONTRACT and MUST be computed against a specific identity version of that contract. | provenance/verified-build, impact/direct-impact |
| impact/direct-impact | MUST | An impact finding whose hop depth is exactly 1 MUST include DIRECT in its impact type set, MUST carry a path whose node count exceeds its step count by one, MUST carry evidence on the finding and on every step of its path, and MUST state the direction in which the change propagated for the finding and for each step. An impact finding MUST NOT traverse a relationship whose declared change propagation is none. | impact/direct-impact, invalid/broken-edge |
| impact/transitive-impact | MUST | An impact finding whose hop depth is 2 or more MUST include TRANSITIVE in its impact type set, and MUST additionally include MULTI_HOP when the hop depth is 3 or more. It MUST carry a path with per-step evidence and confidence, and its relationshipTypes MUST list the relationship of each step in path order. Its aggregated confidence MUST equal the minimum confidence ordinal among its steps. An entity kind whose affectedKinds includes the affected entity MUST have its corresponding classification present in the impact type set. | impact/transitive-impact, impact/multi-hop-impact |
| provenance/build-to-wasm | MUST | The correspondence between a build artifact and a deployed executable MUST be established by comparing their digests. A build MUST NOT be recorded as producing an executable when the digests differ, and differing digests MUST be reported as CONFLICTING rather than as PARTIALLY_VERIFIED. Where a digest comparison cannot be performed because one side is unavailable, the correspondence MUST be recorded as unestablished. | provenance/verified-build, provenance/mismatched-wasm |
| provenance/contract-to-deployment | MUST | A deployment record MUST name its contract, network and status. Its status MUST be CONFIRMED only when the deployment transaction is recorded as successful and the resulting contract is observed on the network at the stated boundary. A transaction recorded as unsuccessful MUST NOT support CONFIRMED, an unknown outcome MUST be recorded as UNCONFIRMED rather than FAILED, and a record that names a transaction MUST also name its ledger. A network descriptor MUST declare the passphrase of the network it identifies. | provenance/verified-build, minimal/provenance, invalid/malformed-contract |
| provenance/evidence-traceability | MUST | Every evidence record MUST state the specific claim it supports and MUST carry the fields its class requires for that claim to be checkable. A claim MUST reference at least one evidence record and MUST NOT rest on an evidence record whose claim is broader than the claim being made. Evidence derived from a transaction recorded as unsuccessful MUST NOT be cited as supporting the effect that transaction attempted. | minimal/provenance, provenance/unknown-source, invalid/invalid-provenance |
| provenance/snapshot-consistency | MUST | A snapshot that repeats its network and ledger boundary at the top level MUST have those fields equal the corresponding fields of its observation boundary. A snapshot MUST declare the specification version that produced it, and MUST declare which fields are excluded from its content digest. | minimal/provenance |
| provenance/source-to-build | MUST | A build record MUST state the source revision it consumed, and a source revision reached through a mutable ref MUST NOT be reported as VERIFIED. When a build cannot record a toolchain identity, the provenance status MUST be PARTIALLY_VERIFIED or lower, and the confidence MUST carry evidence for the missing component rather than omitting it. When any evidence contradicts the claim, the status MUST be CONFLICTING. | provenance/verified-build, provenance/partial-build, provenance/mismatched-wasm |
| provenance/wasm-to-contract | MUST | The executable a contract hosts MUST be established from a network observation that carries its observation boundary, and the contract identity MUST be versioned so that a later change of executable is a new identity rather than a contradiction. A document MUST NOT assume that the executable currently at an address is the executable that was deployed there, and MUST record any observed upgrade in the identity's upgrade history with its own evidence. | provenance/verified-build, provenance/mismatched-wasm, provenance/unknown-source |
